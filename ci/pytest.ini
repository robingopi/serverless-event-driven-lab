[pytest]
pythonpath = .
markers =
    unit: fast isolated unit tests (no real AWS calls)
    integration: tests using moto-mocked AWS services (DynamoDB, EventBridge)
    smoke: tests against a real deployed API Gateway endpoint
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
